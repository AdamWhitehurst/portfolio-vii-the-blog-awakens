import styled from 'styled-components'

export default styled.label`
  margin: 4px;
  white-space: nowrap;
  color: var(--accent);
  opacity: 1;
  font-size: 1rem;
  line-height: 1.4rem;
  font-weight: 300;
`
