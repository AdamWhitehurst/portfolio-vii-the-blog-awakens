import styled from 'styled-components'

export default styled.label`
  margin: 0.2rem;
  white-space: nowrap;
  color: var(--accent);
  opacity: 1;
  font-size: 1.2rem;
  line-height: 1.4rem;
  font-weight: 700;
`
